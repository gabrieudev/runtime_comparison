FROM node:20-alpine
WORKDIR /app
RUN apk add --no-cache curl
COPY src/server.mjs ./server.mjs
ENV PORT=3000
ENV RUNTIME_NAME=node
EXPOSE 3000
HEALTHCHECK --interval=10s --timeout=3s --start-period=10s --retries=3 \
  CMD curl -f http://localhost:3000/ping || exit 1
CMD ["node", "server.mjs"]
